<header class="bg-white dark:bg-gray-800 shadow-sm">
    <div class="max-w-container mx-auto px-4 py-3">
        <div class="flex justify-between items-center">
            <a href="/" class="md:text-2xl  text-lg font-bold text-primary dark:text-indigo-400 transition">Ticketr</a>
            <div class="flex items-center gap-4">
                <span class="text-gray-500 dark:text-gray-300">Welcome, <strong>{{ user.name | default('User') }}</strong></span>
                <div class="border-l border-gray-300 dark:border-gray-600 h-6"></div>
                {% include 'components/theme_toggle.twig' %}
                <a href="#" id="logout-button" class="bg-primary hover:bg-primary-light text-white px-4 py-2 rounded-lg transition focus:outline-none focus:ring-2 focus:ring-primary-light focus:ring-offset-2">Logout</a>
            </div>
        </div>
    </div>
</header>
